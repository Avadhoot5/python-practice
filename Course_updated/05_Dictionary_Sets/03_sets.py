# sets - collection of non-repetitive elements

# Properties 
# Unordered - element's order doesn't matter, unindexed - cannot access elements by index, no way to change items in sets.

# creates an empty set 
emptySet = set()

print(emptySet, type(emptySet))

my_set = {1, 2, 3, "apple", "banana", "cherry"}

print(my_set, type(my_set))


